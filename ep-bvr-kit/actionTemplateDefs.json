{
  "epDeleteAction": {
    "actionType": "JSFunctionAsync",
    "method": "deleteObjects",
    "inputData": {
      "objectsToDelete": "{{inputData.objectsToDelete}}",
      "connectToObject": "{{inputData.connectToObject}}",
      "relatedDataset": "{{inputData.relatedDataset}}",
      "reloadType": "{{inputData.reloadType}}",
      "reloadProperties": "{{inputData.reloadProperties}}",
      "additionalLoadParams": "{{inputData.additionalLoadParams}}"
    },
    "deps": "js/epDeleteService"
  },
  "epCopyAction": {
    "actionType": "JSFunction",
    "method": "copy",
    "inputData": {
      "objectToCopy": "{{inputData.objectsToCopy}}"
    },
    "deps": "js/epCutCopyService"
  },
  "epCutAction": {
    "actionType": "JSFunction",
    "method": "cut",
    "inputData": {
      "objectToCopy": "{{inputData.objectsToCopy}}"
    },
    "deps": "js/epCutCopyService"
  }
}
